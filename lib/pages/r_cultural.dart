import 'package:flutter/material.dart';

class Rcultural extends StatelessWidget {
  const Rcultural({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('R Cultural'),
      ),
      body: Center(
        child: 
          Text("Hola mundo"),
        )
    );
  }
}